# Core

This directory is reserved for Codex-native orchestration logic.

The first bootstrap pass keeps executable logic minimal and moves only the portable runtime helpers and state conventions needed for campaigns and fleet coordination. Router logic, runtime bootstrap, and higher-level orchestration code should land here as the Codex integration solidifies.
