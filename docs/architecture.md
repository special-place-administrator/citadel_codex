# Architecture

Citadel Codex keeps Citadel's orchestration model but replaces the Claude-specific integration layer.

## Orchestration Ladder

1. Direct execution: small, bounded work in the active session.
2. Skills: focused markdown workflows for repeatable tasks.
3. Archon: multi-session campaign orchestration with persistent state.
4. Fleet: wave-based parallel execution with shared discovery briefs.

The rule is unchanged from upstream Citadel: use the cheapest level that fits the work.

## Repository Layout

| Directory | Purpose |
|-----------|---------|
| `core/router/` | Intent classification and routing (`classify-intent.js`) |
| `skills/` | Portable Codex-oriented skill definitions (9 installed) |
| `runtime/cli.js` | CLI entrypoint — `init`, `status`, `continue`, `setup`, `route` |
| `runtime/bootstrap/` | State initialization (`init-state.js`) and template sync (`sync-templates.js`) |
| `runtime/commands/` | Command implementations: `status.js`, `continue.js`, `setup.js` |
| `runtime/checks/` | Explicit verification: `post-edit.js`, `quality-gate.js`, `circuit-breaker.js` |
| `runtime/scripts/` | Coordination, discovery compression, and handoff parsing utilities |
| `.citadel/` | Neutral runtime state and templates |
| `docs/` | Architecture, runtime, command surface, and migration documentation |
| `tests/` | Smoke tests for runtime scripts |

## State Model

Persistent state lives under `.citadel/`:

- `.citadel/campaigns/`: active and completed campaign files.
- `.citadel/fleet/`: wave outputs and compressed discovery briefs.
- `.citadel/coordination/`: file-based scope claims and live instances.
- `.citadel/intake/`: optional queued work items.
- `.citadel/templates/`: campaign, fleet session, and intake templates.
- `.citadel/telemetry/`: circuit breaker state and runtime metrics.
- `.citadel/config.json`: project stack detection and skill registry (generated by `setup`).

This keeps state portable and avoids `.claude/` assumptions.

## Command Surface

All interaction is via explicit CLI commands — no hook bus, no slash commands:

```
node runtime/cli.js init       # Create .citadel/ state tree
node runtime/cli.js setup      # Detect stack, generate config
node runtime/cli.js status     # Show active campaigns/fleet/skills
node runtime/cli.js continue   # Resume active campaign or fleet session
node runtime/cli.js route "x"  # Classify intent and show routing target
```

See [command-surface.md](command-surface.md) for details.

## Runtime Checks

Explicit invocations replacing Claude's hook-based verification:

- `post-edit.js --path <file>`: language-adaptive type checking and lint.
- `quality-gate.js [--scope <dir>]`: anti-pattern scanning on git-changed files.
- `circuit-breaker.js --record-failure|--status|--reset`: failure-loop detection.

See [runtime.md](runtime.md) for details.

## Intent Router

The router (`core/router/classify-intent.js`) classifies input using three tiers:

1. **Tier 0 — Pattern Match**: keyword/regex on raw input (~0 cost).
2. **Tier 1 — Active State**: checks `.citadel/campaigns/` and `.citadel/fleet/` for active work.
3. **Tier 2 — Skill Registry**: matches input against `skills/*/SKILL.md` frontmatter.

The upstream LLM classifier (Tier 3) is deferred — Tiers 0-2 are sufficient for the current skill set.

## Runtime Scripts

Three imported utility scripts from upstream Citadel:

- `coordination.js`: file-based scope claiming for concurrent work.
- `compress-discovery.cjs`: brief generation from agent outputs.
- `parse-handoff.cjs`: extraction of structured HANDOFF blocks.
